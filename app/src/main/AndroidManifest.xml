<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.igluesmik.sopt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan"
        android:usesCleartextTraffic="true">

        <activity android:name=".ui.view.profile.DetailWebActivity"></activity>
        <activity android:name=".ui.view.profile.DetailActivity" />
        <activity android:name=".ui.view.profile.ProfileActivity" />
        <activity android:name=".ui.view.login.SignUpActivity" />
        <activity android:name=".ui.view.login.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.view.main.MainActivity" />
    </application>

</manifest>